# 2025년 12월 22일 월요일 일일 작업일지

## 오늘 한 일

*   **하이브리드 증강 시스템 구현:**
    *   무기 스탯을 중앙 집중식으로 관리하는 `WeaponStats.cs` 클래스 생성.
    *   스탯 수정(`ModifyStats`) 및 공격 효과(`OnAttack`)를 포함하도록 `IAugment.cs` 인터페이스 확장.
    *   베이스 `Weapon.cs` 클래스를 증강 및 스탯 시스템에 맞게 리팩토링.
    *   `Projectile.cs`와 `ProjectileWeapon.cs`를 수정하여 데미지가 실시간으로 반영되고 최종 스탯(`finalStats`)을 사용하도록 리팩토링.
    *   `AoEWeapon.cs`를 최종 스탯(`finalStats`)을 사용하도록 리팩토링하고, `OnTriggerExit` 로직을 추가하여 범위 밖으로 나간 적을 올바르게 처리하도록 개선.
    *   데미지 증가(`DamageUpAugment.cs`) 및 투사체 수 증가(`ProjectileCountUpAugment.cs`) 예제 증강 클래스 생성.

*   **`NullReferenceException` 버그 수정:**
    *   `Assets/02.Scripts/AutoAttack/AutoAttack.cs` 파일에서 `AddWeapon` 메서드 내의 중복된 `WeaponSettingLogic()` 호출 제거.
    *   `Assets/02.Scripts/Cotroller/PlayerController.cs` 파일에서 `autoAttacker.GameStart()` 호출을 `Awake()`에서 `Start()` 메서드로 이동하여 Unity 실행 순서 문제를 해결.

## 내일 할 일

*   사용자의 다음 지시 대기 중.

## 현재 직면한 문제점

*   없음. 오늘 발생한 `NullReferenceException` 문제는 해결되었습니다.
