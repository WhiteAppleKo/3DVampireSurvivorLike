# 2026-01-14 일일 작업 일지

## 📝 오늘 진행한 일
- **SaveManager 데이터 갱신 로직 설계**
    - `ISaveable` 인터페이스를 활용하여 저장 시점에 등록된 객체들이 스스로 데이터를 채우는 구조 제안.
    - `saveList` 순회를 통한 효율적인 데이터 취합 로직 정립.
- **데이터 로딩 안정성 분석**
    - '1초 딜레이' 방식의 위험성(야매 방식)에 대해 논의하고, 스크립트 실행 순서 제어 및 이벤트 기반의 정석적인 대안 제시.
- **PlayerController 증강 복구 시스템 설계**
    - 저장된 증강 ID 리스트를 `StatAbilityDatabase`와 매핑하여 실제 증강 객체로 복구하는 로직 설계.
    - 증강 복구 후 최종 스탯(`RecalculateStats`) 및 현재 수치(HP, EXP) 동기화 계획 수립.

## ❓ 직면했던 문제점
- `PlayerController` 내부에서 `SaveManager`의 존재하지 않는 메서드를 참조하고 있던 부분을 `CurrentSaveData`를 직접 참조하도록 수정 방향 설정.
- 증강 데이터를 ID로 관리할 때, 이를 런타임에 다시 객체화하기 위한 데이터베이스 접근 권한(Database Reference) 문제 확인.

## 💡 새롭게 배운 점
- 유니티 프로젝트에서 비동기 로딩이나 초기화 순서 문제를 해결하기 위해 시간 딜레이를 사용하는 것이 장기적으로 시스템에 미치는 부정적인 영향(유지보수 저하, 성능 낭비 등)에 대해 깊이 이해함.

---
**작업 위치:** `C:\Users\kot77\Documents\3DVampireSurvivorLike`
**작성자:** Gemini CLI Agent